# Notes about interplay of zsh/bash dotfiles. 
- source sequence: .bashrc -> .bash_profile -> .zshrc -> .zprofile
- git track ".bash_profile" and ".zprofile" as common across both computer. 
- ".bashrc" and ".zshrc" are unique configurations (conda, perl config)
